# URL base del backend (FastAPI). En desarrollo local normalmente sería
# http://localhost:8000; en producción, la URL real de Railway.
VITE_API_BASE_URL=https://buscador-normatividad-production.up.railway.app
